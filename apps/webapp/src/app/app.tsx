import styled from '@emotion/styled';

const Layout = styled.div`
  margin: 32px auto;
  text-align: center;
  width: 600px;
`;

export const App = () => {
  return (
    <Layout>
      cebulendarz
    </Layout>
  );
}
